---
const base = Astro.base || '';
const navItems = [
  { href: `${base}/`, label: 'Главная' },
  { href: `${base}/hotel`, label: 'Гостиница' },
  { href: `${base}/restaurant`, label: 'Ресторан' },
  { href: `${base}/amenities`, label: 'Услуги' },
  { href: `${base}/contact`, label: 'Контакты' },
];

const currentPath = Astro.url.pathname;
---

<header class="bg-white shadow-md sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-4">
    <div class="flex items-center justify-between h-20">
      <a href={`${base}/`} class="flex items-center gap-3">
        <div class="w-12 h-12 bg-primary rounded-full flex items-center justify-center">
          <span class="text-white font-bold text-xl font-playfair">Б</span>
        </div>
        <div class="hidden sm:block">
          <span class="font-playfair text-xl font-bold text-primary">Бельмонт</span>
          <span class="block text-xs text-gray-500">Гостиничный комплекс</span>
        </div>
      </a>

      <nav class="hidden lg:flex items-center gap-8">
        {navItems.map((item) => (
          <a 
            href={item.href} 
            class={`text-sm font-medium transition-colors hover:text-primary ${
              currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href))
                ? 'text-primary' 
                : 'text-charcoal'
            }`}
          >
            {item.label}
          </a>
        ))}
      </nav>

      <div class="hidden md:flex items-center gap-4">
        <a href="tel:+79068628866" class="text-sm font-semibold text-primary hover:text-primary-dark">
          8 (906) 862-88-66
        </a>
        <a 
          href="https://tl.hotelote.ru/widget?hotelId=102988" 
          target="_blank"
          rel="noopener noreferrer"
          class="bg-primary text-white px-4 py-2 rounded-lg text-sm font-medium hover:bg-primary-dark transition-colors"
        >
          Забронировать
        </a>
      </div>

      <button id="mobile-menu-btn" class="lg:hidden p-2" aria-label="Меню">
        <svg class="w-6 h-6" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M4 6h16M4 12h16M4 18h16" />
        </svg>
      </button>
    </div>
  </div>

  <div id="mobile-menu" class="hidden lg:hidden bg-white border-t">
    <nav class="max-w-7xl mx-auto px-4 py-4 flex flex-col gap-3">
      {navItems.map((item) => (
        <a 
          href={item.href} 
          class={`text-sm font-medium py-2 ${
            currentPath === item.href || (item.href !== '/' && currentPath.startsWith(item.href))
              ? 'text-primary' 
              : 'text-charcoal'
          }`}
        >
          {item.label}
        </a>
      ))}
      <div class="flex flex-col gap-3 pt-3 border-t">
        <a href="tel:+79068628866" class="text-sm font-semibold text-primary">
          8 (906) 862-88-66
        </a>
        <a 
          href="https://tl.hotelote.ru/widget?hotelId=102988" 
          target="_blank"
          rel="noopener noreferrer"
          class="bg-primary text-white px-4 py-2 rounded-lg text-sm font-medium text-center hover:bg-primary-dark transition-colors"
        >
          Забронировать
        </a>
      </div>
    </nav>
  </div>
</header>

<script>
  const menuBtn = document.getElementById('mobile-menu-btn');
  const mobileMenu = document.getElementById('mobile-menu');
  
  menuBtn?.addEventListener('click', () => {
    mobileMenu?.classList.toggle('hidden');
  });
</script>
