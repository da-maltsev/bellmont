---
interface Props {
  title: string;
  description?: string;
  href: string;
  icon?: string;
}

const { title, description, href, icon } = Astro.props;
---

<a 
  href={href}
  class="group flex flex-col bg-white rounded-2xl p-6 border border-gray-100 hover:border-primary/30 transition-colors duration-300"
>
  {icon && (
    <div class="w-14 h-14 bg-cream rounded-lg flex items-center justify-center mb-4">
      <Fragment set:html={icon} />
    </div>
  )}
  <h3 class="font-playfair text-xl font-semibold text-primary mb-2 group-hover:text-primary-dark transition-colors">
    {title}
  </h3>
  {description && (
    <p class="text-gray-600 text-sm line-clamp-2">
      {description}
    </p>
  )}
  <span class="inline-flex items-center gap-1 text-primary text-sm font-medium mt-auto pt-4 group-hover:gap-2 transition-all">
    Подробнее
    <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 5l7 7-7 7" />
    </svg>
  </span>
</a>
